const Dashboard = () => {
    return (
        <div style={{ padding: '50px', color: 'black' }}>
            <h1>DEBUG: DASHBOARD IS RENDERING</h1>
            <p>If you can see this, the routing works.</p>
        </div>
    );
};

export default Dashboard;
