# Frontend Git Commands

Run these commands in your terminal inside the `frontend` folder:

```bash
git init
git add .
git commit -m "Initial frontend commit"
git branch -M main
git remote add origin https://github.com/Mohityadav1541/sanwaliya-frontend.git
git push -u origin main
```
