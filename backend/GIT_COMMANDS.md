# Backend Git Commands

Run these commands in your terminal inside the `backend` folder:

```bash
git init
git add .
git commit -m "Initial backend commit"
git branch -M main
git remote add origin https://github.com/Mohityadav1541/sanwaliya-backend.git
git push -u origin main
```
